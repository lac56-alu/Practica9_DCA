#include <stdlib.h>
#include <iostream>
using namespace std;
#include <stdio.h>

int main() {
    float x, y, resultado;
    char op;
    int valido = 1;

    cout << "Ingrese operacion: ";
    cin >> op;
    cout <<"Ingrese x: ";
    cin >> x;
    cout << "Ingrese y: ";
    cin >> y;
}